package com.example.android.sixsigma.tensorflowfridge;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.TextView;
//Darrin Cation
public class Generate_barcode extends AppCompatActivity {

    private static Button getTheInfo;
    private static TextView display;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_generate_barcode);
/*
        //display = (TextView) findViewById(R.id.Display);
        getTheInfo = (Button) findViewById(R.id.getInfo);
        getTheInfo.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View view) {



            }


        });
*/
    }
}
